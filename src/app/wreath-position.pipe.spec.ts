import { WreathPositionPipe } from './wreath-position.pipe';

describe('WreathPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new WreathPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
